spring:
  jpa:
    # JPA Settings
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        
    defer-datasource-initialization: true

  # 테스트 db 이름 설정
  datasource:
    url: jdbc:h2:mem:testdb

  # H2 활성화
  h2:
    console:
      enabled: true
